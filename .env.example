# =============================================================================
# QalNet Monorepo — Root Environment Example
# App-specific env vars live in apps/backend/.env and apps/web/.env.local
# =============================================================================

# Turborepo remote caching (optional — connect via `npx turbo login`)
# TURBO_TOKEN=
# TURBO_TEAM=
